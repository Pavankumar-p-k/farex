# Android Flutter Client (Scaffold)

This folder provides a starter app for:
- Hybrid route resolution (local/cloud)
- REST auth + heartbeat + recognition submit
- WebSocket live event updates

Camera + ArcFace embedding integration is intentionally left as a production integration step.

